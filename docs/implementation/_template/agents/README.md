# Agent handoffs

Create one directory per stable agent ID and append dated handoff/activity
summaries in `handoff.md`. Include the actual assignment, delivered files,
checks, blockers, decisions, and sign-off state. Link canonical status and
progress records instead of duplicating them. Do not fabricate unavailable
transcripts or expose private reasoning.
