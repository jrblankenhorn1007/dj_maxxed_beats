# User request record

The user asked for an `implementation` folder under `docs/`, with a dossier
for each branch and a dedicated `code-review` folder per branch. The dossiers
must make branch-specific prompts, agent handoffs/logs, code-review reports,
and decisions discoverable; the project documentation must explain the
process for future work.

The earlier shared workflow work already added independent Ralph Code
Reviewer and conditional Ralph Security Reviewer agents. The active review
limit is two completed rounds: an initial review and, if needed, one
follow-up; after the follow-up the author acts on that report alone without a
third reviewer pass.

This is a concise scope summary of the relevant user messages, not a
verbatim transcript of the full conversation.
