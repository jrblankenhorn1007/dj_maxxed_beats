Update the dj_maxxed_beats product plan and acceptance documentation for Windows 10 and 11 (and the latest supported Windows release), macOS (latest supported, retaining the actual MacBook Neo device gate), and Ubuntu Linux (current supported LTS). Rename the model selector in the in-app editor to exactly "Choose your DJ" while keeping provider/model identity explicit. Add to the product goal a persistent, project-local library for AI-generated rendered sound files, with safe unique names, no overwrite, useful non-secret metadata, and restart persistence. Also document a safe Ralph recovery for a verified GH013 status/lease denial explicitly requiring API/UI: use a fresh isolated status-only PR, retain required checks/review, verify remote main, and never retry direct main writes or claim publication before merge. Implement and validate the documentation changes, use only authorized PR integration, verify remote merges, and complete the required post-merge memory review.
